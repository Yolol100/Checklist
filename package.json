{
  "name": "webactueel-checklist-qa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only MCP QA runner for the Webactueel website checklist workflow.",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/checklist.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.4",
    "express": "^5.1.0",
    "zod": "^3.25.76"
  },
  "engines": {
    "node": ">=20"
  }
}
